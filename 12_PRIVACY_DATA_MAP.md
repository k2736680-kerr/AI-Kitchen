# 12 — Privacy Data Map

> 本文是 AI Kitchen 数据收集、使用、存储、共享、保留和删除的工程地图。它用于让代码、隐私政策、应用商店申报和第三方供应商配置保持一致，不构成针对特定法域的法律意见。

| 属性 | 内容 |
|---|---|
| 文档状态 | Draft / Requires Legal & Vendor Review Before Release |
| 实施状态 | 尚未建立生产数据、供应商合同或正式隐私政策 |
| 默认原则 | 数据最小化、目的限制、最小权限、环境隔离、可删除 |
| 依赖 | `05_AUTH_AND_IDENTITY.md`、`09_FOOD_SAFETY_RULES.md`、`20_DEPLOYMENT_AND_RELEASE.md` |
| 最后更新 | 2026-07-27 |

---

## 1. 目标

- 列出产品处理的每类数据；
- 明确数据来自哪里、为什么需要、存在哪里、谁可访问；
- 明确是否发送第三方 AI、监控、构建或通知供应商；
- 定义保留、删除、导出和账户升级迁移；
- 约束日志、分析、测试和客服数据；
- 支持应用商店 Data Safety/Privacy Nutrition Label 填报；
- 让架构变化在上线前触发隐私审查。

---

## 2. 数据主体与身份

| 身份 | 标识 | 云端数据 | 说明 |
|---|---|---|---|
| guest | 本地随机 guest ID | 默认无 | 仅设备内体验 |
| anonymous | Supabase anonymous user ID | 有 | 可升级正式账号 |
| registered | Supabase auth user ID | 有 | 邮箱或第三方登录 |
| support/admin | 受控内部身份 | 最小必要访问 | 操作需审计 |

客户端传来的 ownerId 不可信。服务端所有权由验证后的 subject 建立，数据库通过 RLS 强制隔离。

---

## 3. 数据分类

### 3.1 账户数据

- auth user ID；
- 邮箱或第三方身份标识；
- 认证 provider；
- 会话 Token（只在安全存储中）；
- 创建、登录、删除时间。

密码由认证供应商处理，业务数据库不得保存明文密码。

### 3.2 偏好与约束

- 默认人数、时间、厨具；
- 辣度、难度、饮食目标；
- 忌口、过敏原 code；
- 可选高风险人群约束。

过敏和高风险人群信息属于高敏感业务数据：只为生成和安全校验使用，不用于广告画像。

### 3.3 用户内容

- 选择和手动输入的食材；
- 生成条件；
- 菜谱、收藏、历史；
- 反馈文本；
- 烹饪进度；
- 可选上传内容（后续功能，首版无照片）。

### 3.4 技术数据

- requestId、idempotencyKey；
- App 版本、OS、设备类别；
- 性能、崩溃、网络和错误 code；
- 模型、Prompt、Schema、Rule 版本；
- Token 用量和估算成本；
- 安全 finding codes。

### 3.5 不应收集

- 精确位置（首版不需要）；
- 联系人、短信、通话记录；
- 广告 ID（无广告时不需要）；
- 麦克风和相机（首版核心流程不需要）；
- 医疗诊断；
- 完整银行卡或支付凭据；
- 与产品无关的设备文件。

---

## 4. 数据流总览

```text
User Input
  → Mobile validation/local state
  → Own API over TLS
  → Auth & rate limit
  → Input normalization
  → AI Provider (minimum necessary generation payload)
  → Server validation/rules/nutrition
  → PostgreSQL/RLS
  → Mobile cache

Errors/metrics
  → Redaction
  → Observability provider
```

App 不直接发送数据给 AI Provider。服务端负责最小化、脱敏、供应商选择和保留策略。

---

## 5. 数据处理矩阵

| 数据 | 目的 | 本地 | 后端 | 第三方 AI | 监控 | 默认保留 |
|---|---|---:|---:|---:|---:|---|
| guest ID | 本地隔离 | 是 | 否 | 否 | 可用随机安装会话 | App 删除前 |
| auth user ID | 身份/所有权 | 是 | 是 | 否 | 哈希/内部 ID | 账户存在期间 |
| 邮箱 | 登录/支持 | 安全存储少量 | Auth | 否 | 否 | 账户存在期间 |
| 食材与条件 | 生成菜谱 | 草稿/缓存 | 请求与菜谱 | **必要字段** | 仅 code/指标 | 按请求/菜谱策略 |
| 过敏原 code | 安全约束 | 是 | 是 | 最小必要约束 | 不记录原文 | 账户或用户删除前 |
| Recipe | 展示/历史 | 缓存 | 是 | 供应商产生 Candidate | 错误时不记录全文 | 用户删除前 |
| 原始模型输出 | 校验/故障 | 否 | 最短必要 | 供应商处理 | 否 | 最短必要或不保存 |
| request metadata | 幂等/成本 | 少量 | 是 | request tag 可选 | 是 | 90–180 天建议 |
| 崩溃堆栈 | 稳定性 | 临时 | 可选 | 否 | 是 | 30–90 天建议 |
| 反馈文本 | 支持/质量 | 草稿 | 是 | 默认否 | 否 | 处理及政策期限 |

最终期限需结合首发地区、供应商能力和运营需求确认。

---

## 6. 第三方 AI 数据边界

发送内容应限制为：

- 标准化食材及必要原显示名；
- 人数、最大时间、厨具、饮食偏好；
- 过敏和禁止项的最小结构化约束；
- 输出格式和系统规则。

不得发送：

- 邮箱、昵称、Access Token；
- 精确设备 ID；
- owner ID；
- 完整账户历史；
- 与本次生成无关的反馈和菜谱；
- 高风险人群的自由文本诊断描述。

供应商上线前必须确认：训练使用、数据保留、区域、子处理商、删除能力、日志配置和安全条款。实际配置必须与隐私政策一致。

---

## 7. 本地存储

### 7.1 安全存储

仅用于：

- refresh/session token；
- 必要设备密钥；
- 迁移状态的敏感凭据。

### 7.2 普通本地数据库

可保存：

- Recipe 缓存；
- 食材草稿；
- 烹饪进度；
- 同步队列；
- 非敏感配置。

过敏偏好可以缓存用于离线体验，但需要用户命名空间隔离、退出清理和最小化。不得将 Token 存入 AsyncStorage/明文日志。

---

## 8. 服务端存储和访问

- PostgreSQL 表使用 owner_id + RLS；
- Service Role 只在受控服务端；
- 支持人员默认看不到完整用户内容；
- 高风险反馈和安全审计使用受限角色；
- 数据库备份、分析导出和日志同样属于数据处理；
- staging/development 不使用生产用户数据；
- 必要调试使用合成或脱敏数据；
- 所有管理操作记录审计。

---

## 9. 日志与可观测性

### 9.1 允许

- requestId；
- 内部 subject hash；
- endpoint；
- duration；
- error code；
- provider/model/prompt/schema/rule version；
- Token 和成本；
- 标准风险 code；
- App/OS 版本。

### 9.2 禁止

- Access/Refresh Token；
- API Key；
- 完整 Authorization header；
- 完整邮箱；
- 完整过敏自由文本；
- 原始 AI 对话和 Recipe 全文作为普通日志；
- SQL 参数中的敏感内容；
- 未脱敏崩溃 breadcrumbs。

建立集中 redaction utility 和日志测试，不能依赖每个开发者手动记住。

---

## 10. 分析数据

P0 可以不接入行为分析。若引入：

- 先定义事件目录；
- 只收集实现产品目标所需字段；
- 不发送完整食材、菜谱和过敏信息；
- 不使用广告追踪 SDK；
- 明确用户选择和地区要求；
- 记录供应商、保留和跨境处理；
- 事件 Schema 版本化；
- development 事件与 production 分离。

示例安全事件：`recipe_generation_completed`，属性仅包含时长区间、结果 code、版本和是否收藏，不包含菜谱正文。

---

## 11. 保留策略

建议初始策略：

| 数据 | 建议期限 | 删除触发 |
|---|---|---|
| 用户 Recipe/收藏/偏好 | 用户删除或账户删除前 | 用户操作/账户删除 |
| generation 结构化日志 | 90–180 天 | 定时任务 |
| 原始模型输出 | 不保存或最短必要 | 定时任务/请求完成 |
| 崩溃和错误 | 30–90 天 | 供应商/后台策略 |
| 安全反馈 | 处理与法定需要 | 受控策略 |
| 删除审计 | 最小证明期限 | 政策到期 |
| 数据库备份 | 固定轮换周期 | 自动过期 |

期限必须实际落入数据库清理任务、供应商设置和备份策略，不能只写在隐私政策。

---

## 12. 账户删除

删除流程至少包括：

1. 重新验证或确认当前身份；
2. 创建 deletion request；
3. 禁止新增生成；
4. 删除/匿名化 profiles、preferences、recipes、favorites；
5. 处理 feedback 和法定保留；
6. 撤销 Auth 身份与会话；
7. 删除本地缓存和安全存储；
8. 将不可立即删除的备份数据设为自然过期且不可恢复到活跃系统；
9. 返回删除状态；
10. 记录不含敏感正文的完成证明。

账户删除不得只删 `profiles`。失败需要可恢复状态机，避免部分删除后重新暴露数据。

---

## 13. Guest/Anonymous 升级

- guest 数据默认本地；
- 升级 anonymous 时创建 auth subject；
- 批量上传使用幂等 migration ID；
- 上传前建立本地快照；
- 校验数量和 checksum；
- 成功后再标记迁移完成；
- 登录已有账号时不得自动合并另一个人的本地数据，需明确用户选择；
- 切换账号清空或隔离前一用户命名空间。

---

## 14. 数据主体请求

发布前应准备：

- 访问/导出；
- 更正；
- 删除；
- 撤回可选同意；
- 供应商数据请求的转交流程；
- 身份验证；
- 响应记录。

首版产品可优先实现账户内可见数据导出和删除，其他请求通过支持渠道处理，但政策必须真实反映能力。

---

## 15. 儿童与年龄

若产品不面向儿童：

- 商店年龄分级和文案保持一致；
- 不主动收集儿童年龄；
- 不将“给儿童做饭”自动等同于用户是儿童；
- 高风险人群约束作为菜谱安全条件处理；
- 未来如面向儿童，必须重新评估同意、数据最小化、分析 SDK 和内容设计。

---

## 16. 环境和测试数据

- production 数据不得复制到 development；
- staging 使用合成用户和固定 Recipe；
- AI 评估集不得包含真实用户可识别内容；
- 错误复现优先用 request metadata 和脱敏 Snapshot；
- 数据导出必须受控、限时和审计；
- 自动化测试账户按环境清理；
- 截图和录屏可能含用户内容，进入 issue 前必须脱敏。

---

## 17. 供应商清单模板

| 供应商 | 用途 | 数据 | 地区 | 保留 | 训练/广告 | DPA/条款 | 状态 |
|---|---|---|---|---|---|---|---|
| Supabase | Auth/DB/Functions | 账户与业务数据 | 待确认 | 待配置 | 不适用 | 待审 | Proposed |
| AI Provider | 菜谱 Candidate | 最小生成输入 | 待确认 | 待配置 | 必须关闭或披露 | 待审 | Proposed |
| Sentry/同类 | 崩溃监控 | 技术数据 | 待确认 | 30–90 天 | 不用于广告 | 待审 | Proposed |
| EAS | 构建/更新 | 构建元数据 | 待确认 | 按供应商 | 不适用 | 待审 | Proposed |

供应商未最终确定前不得在隐私政策写死错误名称；确定后必须更新本文件、政策和商店申报。

---

## 18. 隐私变更触发器

以下改动必须先审查本文件：

- 新增相机、麦克风、照片、位置；
- 新增登录 provider；
- 更换 AI/监控/分析供应商；
- 引入广告、订阅或支付；
- 进入新国家/地区；
- 保存健康、年龄或家庭数据；
- 延长保留周期；
- 使用生产数据训练或评估；
- 新增后台运营访问；
- 新增推送和营销；
- 数据导出到数据仓库。

---

## 19. 安全控制

- TLS；
- Secrets 管理；
- RLS 和 API 授权；
- 最小权限；
- 安全存储；
- 日志脱敏；
- 依赖和密钥扫描；
- 管理操作审计；
- 备份和恢复；
- 账户删除；
- 事件响应；
- 供应商审查。

隐私和安全不是同一概念，但必须联合设计。

---

## 20. 商店申报映射

发布前逐项核对：

- 收集哪些数据类型；
- 数据是否关联身份；
- 是否用于 App 功能、分析、开发者通信、广告；
- 是否与第三方共享；
- 是否加密传输；
- 用户是否可删除；
- 是否有追踪；
- iOS Privacy Manifest/SDK 声明；
- Android Data Safety；
- 隐私政策页面与 App 内入口。

商店表单不是一次性文案，SDK 或数据流变化后必须更新。

---

## 21. 测试与审计

- 用户 A 不能读取用户 B 数据；
- 退出/切换账号后缓存隔离；
- Token 不进入日志；
- AI 请求不含邮箱/owner ID；
- 账户删除覆盖所有表和本地数据；
- 保留任务真正删除过期记录；
- 供应商保留设置与文档一致；
- staging 无生产数据；
- 崩溃报告脱敏；
- 导出文件访问受控且过期；
- 商店申报与实际 SDK 扫描一致。

---

## 22. Definition of Done

- [ ] 首发地区已确认并完成法律审查；
- [ ] 所有生产供应商和子处理商已登记；
- [ ] 每类数据的目的、位置、共享和保留已确认；
- [ ] AI 数据最小化已通过请求级测试；
- [ ] Token/Key/邮箱不进入日志；
- [ ] RLS 和跨用户测试通过；
- [ ] 账户删除端到端通过；
- [ ] 本地缓存和账号切换隔离通过；
- [ ] 保留清理任务和备份轮换启用；
- [ ] 隐私政策、App 内说明和商店申报一致；
- [ ] 新数据流变更门禁进入开发流程。

---

## 23. 当前结论

AI Kitchen 应以“只收集完成核心功能真正需要的数据”为默认。最敏感的过敏和人群约束只用于生成与安全，不用于广告和泛分析。任何新增 SDK、供应商、权限或数据用途，都必须先更新数据地图，再更新实现、政策和商店申报；不能等审核前临时补文案。
