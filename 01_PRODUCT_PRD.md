# 01 — Product Requirements Document

> 本文定义 AI Kitchen 的用户问题、产品范围、功能需求、业务规则、状态、非功能要求和验收标准。技术实现以 `02_ARCHITECTURE.md` 和后续专项文档为准。

| 属性 | 内容 |
|---|---|
| PRD 版本 | 1.0 |
| 产品阶段 | 设计基线 |
| 优先平台 | Android |
| 首发形态 | 移动 App |
| 最后更新 | 2026-07-24 |

---

## 1. 产品概述

### 1.1 产品愿景

让没有烹饪经验或不知道吃什么的用户，基于手头已有食材，在一分钟内获得可以真正执行的菜谱。

### 1.2 核心价值

- 降低“今天吃什么”的决策成本；
- 优先消耗已有食材，减少浪费；
- 将 AI 输出约束为结构化菜谱；
- 通过分步模式和计时器降低烹饪门槛；
- 将过敏、忌口、时间和厨具作为强约束；
- 对生成、失败、安全和成本进行追踪。

### 1.3 核心产品原则

1. 过敏与安全优先于创意；
2. 可执行优先于文字丰富；
3. 已有食材优先于额外采购；
4. 明确失败优先于不可信结果；
5. 不登录也能体验核心价值；
6. 用户数据最小化收集；
7. AI 结果始终允许反馈和纠正。

---

## 2. 目标用户

### 2.1 独居上班族

痛点：时间少、食材少、厨具有限。

需要：10–30 分钟、步骤少、少洗锅、可替代食材。

### 2.2 厨房新手

痛点：不知道准备顺序、火候、时间和熟度。

需要：明确动作、数量、时间、风险提示和计时器。

### 2.3 家庭做饭者

痛点：人数变化、口味不同、份量难换算。

需要：人数换算、偏好、忌口、多人份量。

### 2.4 减脂或健身用户

痛点：希望了解大致热量和宏量营养，但不需要医疗级精度。

需要：来源明确的估算、每份数据和置信度。

### 2.5 冰箱清理用户

痛点：剩余食材组合困难，容易浪费。

需要：优先使用现有食材、标记必要缺少项、提供替代建议。

---

## 3. Jobs To Be Done

- 当我下班回家不知道吃什么时，我想快速输入已有食材，获得一份今天能做的菜；
- 当我厨艺不熟时，我想逐步查看操作和时间，避免漏步骤；
- 当我有过敏或忌口时，我想确信系统不会偷偷加入相关成分；
- 当我只有有限厨具时，我想排除无法完成的菜；
- 当我想清理冰箱时，我想尽量使用已有食材；
- 当结果有问题时，我想提交反馈并让团队定位到具体请求；
- 当我再次使用时，我想查看历史、收藏和上次偏好。

---

## 4. 产品边界

### 4.1 明确非目标

- 不提供疾病诊断、治疗或药物建议；
- 不保证营养值达到医疗、实验室或监管精度；
- 不替代官方食品安全机构；
- 不允许用户将清洁剂、药品或未知非食用物作为正常食材生成；
- 首版不做社区、短视频、直播、复杂电商和配送；
- 首版不依赖拍照识别完成核心流程；
- 不复制其他产品的品牌、图片、文案或受保护 UI。

### 4.2 范围外功能池

- 冰箱拍照识别；
- 条码识别；
- 一周菜单；
- 自动购物清单；
- 家庭共享；
- 多语言；
- 语音指导；
- 会员订阅；
- 菜谱图片生成。

这些功能不得插入 P0/P1，除非在 `DECISIONS.md` 中记录范围替换和影响。

---

## 5. 信息架构

### 5.1 底部导航

| Tab | P0 | P1/P2 |
|---|---|---|
| 首页 | 食材选择、搜索、生成入口 | 个性化和快速复用 |
| 历史 | 本地最近记录 | 云端分页、删除、同步 |
| 收藏 | 可作为 P0 延后项 | 云端收藏和筛选 |
| 我的 | 简化设置和说明 | 偏好、账号、隐私、反馈 |

### 5.2 页面清单

- 启动/初始化；
- 首页；
- 食材搜索；
- 手动添加；
- 生成条件；
- 生成中；
- 菜谱详情；
- 烹饪模式；
- 历史；
- 收藏；
- 偏好设置；
- 登录/账号升级；
- 隐私和 AI 数据说明；
- 反馈；
- 账户删除。

---

## 6. 核心用户流程

### 6.1 首次使用

1. 用户打开 App；
2. 查看简短价值说明；
3. 可选择继续作为 guest；
4. 首次使用 AI 前查看 AI 数据发送说明；
5. 进入首页选择食材；
6. 设置生成条件；
7. 生成并查看详情；
8. 可进入烹饪模式；
9. 本地保存最近记录；
10. P1 可升级匿名身份并云同步。

### 6.2 菜谱生成

```text
选择至少一种食材
→ 检查重复和非法输入
→ 设置人数、最大时间、厨具、偏好和过敏
→ 确认生成
→ 显示进度并禁止重复提交
→ 成功：展示结构化详情
→ 阻断：说明安全原因，不展示危险候选
→ 失败：显示可操作错误和重试
```

### 6.3 烹饪

1. 查看准备工作；
2. 开始第一步；
3. 查看当前步骤和剩余步骤；
4. 必要时启动计时；
5. 暂停、继续或切换后台；
6. App 恢复后按时间戳恢复进度；
7. 完成菜谱；
8. 可收藏、反馈或再次生成。

### 6.4 guest 升级

```text
本地 guest 数据
→ 创建/获取 anonymous user
→ 上传历史、收藏、偏好
→ 校验数量
→ 标记迁移完成
→ 用户绑定正式账号
→ 保留同一个 auth user 和 owner_id
```

迁移必须幂等，失败不能清空本地数据。

---

## 7. 功能需求

## 7.1 食材选择

### FR-ING-001 分类浏览

用户可以按类别查看常用食材。

验收：

- 类别和食材可加载；
- 选中状态清晰；
- 选中数量实时更新；
- 切换类别不丢失选择。

### FR-ING-002 搜索

用户可以按显示名称和别名搜索。

验收：

- 忽略首尾空格；
- 支持常见别名；
- 无结果时允许手动添加；
- 搜索结果不重复。

### FR-ING-003 手动添加

用户可以添加暂未收录的自定义食材。

业务规则：

- 长度和字符受限；
- 相同标准食材或相同自定义名称不得重复；
- 明显非食用物进入安全预检；
- 自定义食材保留原显示名，但不自动拥有可靠营养数据。

### FR-ING-004 数量与单位

P0 可只记录“拥有该食材”；P1/P2 可增加数量和单位。

必须区分：

- 用户输入单位；
- 标准单位；
- 估算克重；
- 是否可用于营养计算。

---

## 7.2 生成条件

### FR-CON-001 人数

- 默认值来自偏好；
- 允许 1–12 人；
- 返回页面后保留；
- 生成结果按人数给出用量。

### FR-CON-002 最大时间

- 建议选项：10、20、30、45、60 分钟和自定义；
- 最小和最大值受服务端限制；
- 菜谱总时间不得明显超过限制；
- 若无法满足，应返回明确错误或让用户调整。

### FR-CON-003 厨具

用户可以选择已有厨具，例如炒锅、烤箱、电饭锅、空气炸锅。

结果不得要求未选择且无法替代的关键厨具。普通刀具、碗和基础炉具是否视为默认拥有需在专项文档中明确。

### FR-CON-004 偏好与忌口

支持辣度、难度、饮食目标、素食等一般偏好。

偏好不是绝对安全规则；忌口和过敏的优先级高于偏好。

### FR-CON-005 过敏原

- 用户可选择已知过敏原；
- 服务端重新校验；
- 候选菜谱不得包含过敏原及明确映射的别名；
- 无法确定时阻断或要求确认；
- 过敏提示不得只依赖颜色。

---

## 7.3 生成请求

### FR-GEN-001 提交

- 至少一个有效食材；
- 生成按钮在提交后立即禁用；
- 每次生成创建 `requestId` 和 `idempotencyKey`；
- 相同幂等 Key 不重复计费；
- 用户可取消等待，但取消行为和供应商计费需明确。

### FR-GEN-002 生成中

界面应说明正在生成，并提供：

- 可理解的进度文案；
- 超时处理；
- 取消；
- 网络切换处理；
- 防止重复提交；
- App 恢复后的请求状态查询。

### FR-GEN-003 成功

成功响应必须是符合当前 Recipe Schema 的结构化对象，并包含：

- `requestId`；
- `schemaVersion`；
- 名称和说明；
- 人数、难度和时间；
- 食材和用量；
- 缺少食材；
- 连续步骤；
- 替代建议；
- 提示和警告；
- 安全状态；
- 营养状态和来源。

### FR-GEN-004 失败

不同错误必须区分：

- 输入无效；
- 网络不可用；
- 身份失效；
- 限流；
- 模型超时；
- 模型供应商错误；
- 输出格式无效；
- 安全规则阻断；
- 数据库错误；
- 未知内部错误。

用户提示应说明下一步，而不是只显示“出错了”。

### FR-GEN-005 安全阻断

若候选结果触发 `BLOCK`：

- 不展示候选菜谱正文；
- 返回安全错误码和可公开原因；
- 可允许重新生成；
- 记录规则版本和 requestId；
- 不在客户端暴露内部规则实现细节。

---

## 7.4 菜谱详情

### FR-REC-001 概览

展示：名称、描述、人数、难度、总时间、主要标签和安全状态。

### FR-REC-002 食材

区分：

- 用户已有；
- 需要补充；
- 可选；
- 调味品；
- 替代项；
- 估算数量。

不得默认把所有调味品视为用户已拥有。

### FR-REC-003 步骤

- 至少一步；
- 编号连续；
- 每步动作清楚；
- 可包含时间和火候；
- 不得要求不存在且不可替代的厨具；
- 风险提示靠近相关步骤。

### FR-REC-004 营养

营养状态：

- `database_calculated`；
- `ai_estimated`；
- `unavailable`。

界面必须显示来源、置信度和估算免责声明。不得使用“精准”“医疗级”等表述。

### FR-REC-005 收藏和分享

P1 支持收藏。分享若实现，只分享用户可见菜谱，不包含 requestId、内部日志、过敏配置或其他敏感信息。

---

## 7.5 烹饪模式

### FR-COOK-001 分步导航

- 当前步骤突出；
- 可查看上一/下一步；
- 完成步骤可标记；
- 退出前确认；
- 退出不删菜谱。

### FR-COOK-002 计时器

- 支持单步计时；
- 可暂停和继续；
- 后台恢复；
- App 被回收后恢复；
- 使用绝对时间戳计算；
- 系统时间异常有保护。

### FR-COOK-003 可读性

- 大字体；
- 高对比度；
- 重要警告不依赖颜色；
- 控件可单手操作；
- 可选屏幕常亮。

---

## 7.6 历史和收藏

### FR-HIS-001 历史

- P0 本地最近记录；
- P1 云端分页；
- 支持加载、空、失败、重试；
- 删除后列表和详情一致；
- 删除行为需区分软删除和永久删除。

### FR-FAV-001 收藏

- 同一用户和菜谱只能存在一条收藏关系；
- 离线操作可排队；
- 冲突不静默覆盖用户较新的操作；
- 用户切换时隔离数据。

---

## 7.7 身份与账号

### FR-AUTH-001 Guest

未登录用户可体验核心生成和本地历史，不因登录墙失去首要价值。

### FR-AUTH-002 Anonymous

P1 使用匿名身份保存云端数据和执行 RLS。

### FR-AUTH-003 Registered

P2 支持正式登录方式和跨设备同步。绑定账号时保留匿名用户 ID 或执行可靠所有权迁移。

### FR-AUTH-004 删除账户

用户可以发起删除，系统处理：

- 个人配置；
- 偏好；
- 菜谱和收藏；
- 可删除反馈；
- 合规要求允许保留的最小日志；
- 本地缓存和身份。

删除状态和保留例外必须在隐私政策中说明。

---

## 7.8 反馈

### FR-FBK-001 反馈入口

用户可从菜谱详情或设置提交：

- 不可执行；
- 食材或数量错误；
- 过敏/安全问题；
- 营养问题；
- 不符合偏好；
- 其他。

反馈自动附带当前 `requestId`，但不展示内部敏感日志。

### FR-FBK-002 安全反馈

安全反馈应具备更高优先级和处理流程，可反向加入规则和固定评估集。

---

## 8. 业务规则

### BR-001 食材优先

候选菜谱应尽量使用用户已有食材。若缺少关键食材，必须列入 `missingIngredients`，不得默认为用户拥有。

### BR-002 时间一致

`totalTimeMinutes`、准备时间、烹饪时间和步骤时间应基本一致，并满足用户最大时间约束。

### BR-003 厨具一致

关键步骤所需厨具必须在用户拥有列表或明确的基础厨具集合中。否则提供替代或判定不可执行。

### BR-004 人数和用量

食材用量必须与 `servings` 对应，份量变化不能只改人数文本。

### BR-005 过敏优先

任何过敏映射命中均不得通过普通偏好或替代建议绕过。

### BR-006 安全不可降级

食品安全校验不可用时不返回未校验菜谱。

### BR-007 营养可降级

营养引擎不可用时可以返回已经通过安全校验的菜谱，但显示 `unavailable`。

### BR-008 生成幂等

相同用户和 `idempotencyKey` 只能产生一个生成生命周期和一次模型计费。

### BR-009 历史可还原

历史详情应使用保存时的 Recipe Snapshot，不因后续 Prompt 或 Schema 变化而完全改变。

### BR-010 版本可追踪

每份生成结果记录 provider、model、promptVersion、schemaVersion、ruleVersion 和 calculationVersion（适用时）。

---

## 9. 页面状态要求

所有异步页面必须覆盖：

- 初始；
- 加载；
- 成功；
- 空；
- 失败；
- 重试；
- 离线；
- 权限或身份异常；
- 数据过期或迁移中。

菜谱生成额外覆盖：

- 校验中；
- 生成中；
- 输出校验中；
- 取消；
- 超时；
- 安全阻断；
- 恢复现有请求。

---

## 10. 非功能需求

### 10.1 性能

- 普通生成 45 秒内返回明确结果或错误；
- 模型单次超时建议 30–35 秒；
- 页面交互不被长任务阻塞；
- 本地列表和详情优先使用缓存；
- 关键列表采用分页。

### 10.2 稳定性

- 主要流程无阻断级崩溃；
- App 被系统回收后关键草稿和烹饪进度可恢复；
- 数据库和模型故障不产生重复计费；
- 错误包含可追踪 requestId。

### 10.3 安全和隐私

- 客户端无高权限 Key；
- RLS 默认拒绝；
- 日志不含敏感原文；
- 数据传输使用 HTTPS；
- 用户数据最小化；
- 账户删除和隐私说明一致。

### 10.4 可访问性

- 支持系统字体放大；
- 危险提示有文本或图标；
- 触控目标合理；
- 文本和背景对比可读；
- 主要操作具备可访问标签。

### 10.5 可维护性

- 共享 Schema；
- 模块化目录；
- Prompt 和规则版本化；
- 数据库迁移；
- 自动化测试；
- 状态和决策文档持续更新。

---

## 11. 成功指标

### 内测目标

| 维度 | 目标 |
|---|---|
| 首次核心流程 | 80% 测试用户无需指导完成 |
| 结构化成功率 | 固定测试集 >= 98% |
| 普通生成 | 45 秒内有明确结果或错误 |
| 过敏安全测试 | 零放行 |
| 主要崩溃 | 无阻断级崩溃 |
| 成本 | 单次、用户和每日均可追踪 |
| 反馈定位 | 反馈可通过 requestId 定位 |

### 发布后观察

- 生成转化率；
- 菜谱详情到烹饪模式转化；
- 完成烹饪率；
- 收藏率；
- 次日和七日留存；
- Schema 失败率；
- 安全规则命中和举报率；
- P50/P95；
- Crash-free users；
- 单次和每日成本。

---

## 12. P0 验收场景

1. 用户选择鸡蛋和番茄，设置 1 人、20 分钟，成功得到结构化菜谱；
2. 未选择食材时生成按钮禁用；
3. 搜索无结果时可以手动添加；
4. 重复添加同一食材不会产生重复项；
5. 生成中重复点击不会产生第二次请求；
6. 模型超时时显示可重试错误；
7. 输出缺字段时不导致页面崩溃；
8. 返回编辑条件后，之前选择仍保留；
9. 成功结果进入本地最近记录；
10. Android 模拟器和指定真机完成主流程。

---

## 13. P1 安全验收场景

1. 花生过敏用户的候选中出现花生或明确别名时必须阻断；
2. 用户输入清洁剂作为食材时必须阻断；
3. 只有电饭锅时不得生成必须使用烤箱且无替代的菜；
4. 10 分钟限制下不得生成明显超过时间的复杂菜；
5. 食品安全规则服务异常时不得展示候选；
6. 营养服务异常时可展示安全菜谱并标记不可用；
7. 用户 A 无法读取用户 B 的历史和收藏；
8. 相同幂等 Key 不重复计费；
9. 安全反馈带 requestId 并可定位规则版本；
10. Prompt 注入不能改变输出 Schema 或系统约束。

---

## 14. 开放产品问题

在对应专项阶段确认：

- 首页首批标准食材数量；
- 是否允许用户编辑 AI 菜谱；
- 调味品默认拥有集合；
- P0 是否保留“收藏”Tab 占位；
- 免费生成次数；
- 首发语言和地区；
- 儿童、孕妇和免疫低下用户提示入口；
- 菜谱分享的首版范围；
- 安全阻断向用户展示多少原因；
- 用户是否可以关闭营养展示。

这些开放项不得影响已锁定的安全、密钥、Schema 和数据所有权边界。
