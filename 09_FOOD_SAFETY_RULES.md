# 09 — Food Safety Rules

> 本文定义 AI Kitchen 的食品安全责任边界、风险分类、规则模型、来源治理、失败关闭、用户提示和测试门禁。它不是医疗建议，也不替代当地食品安全机构；它规定产品如何避免把未经验证的 AI 候选结果直接呈现为安全菜谱。

| 属性 | 内容 |
|---|---|
| 文档状态 | Draft / Ready for Domain Review |
| 实施状态 | 尚未建立正式规则数据源、审核流程或运行代码 |
| 默认地区 | 首发地区待确认；规则必须携带 region |
| 依赖 | `08_RULE_ENGINE.md`、`10_RECIPE_SCHEMA.md`、`12_PRIVACY_DATA_MAP.md` |
| 最后更新 | 2026-07-27 |

---

## 1. 核心原则

1. **模型不是安全裁决者。** 模型可以生成候选步骤和候选提示，但不能产生可信 `safetyStatus=passed`。
2. **安全规则失败关闭。** 核心安全规则无法加载、事实不足或执行异常时，不展示候选菜谱。
3. **过敏约束优先于创意。** 明确过敏原命中即 BLOCK，不通过“少量”“可选”“挑出来”降级。
4. **不确定不等于安全。** 无法识别的食材、模糊别名和未知非食物输入必须进入确认或阻断流程。
5. **地区和人群显式化。** 温度、食材、储存和高风险人群提示可能因地区指南不同而不同。
6. **证据可追溯。** 每条规则有来源、审核、版本、生效时间和适用范围。
7. **提示不只依赖颜色。** BLOCK/WARN 必须有文字、图标/结构和可访问性标签。
8. **不做医疗诊断。** 产品不能根据健康输入诊断疾病或给出治疗性饮食方案。

---

## 2. 产品责任边界

### 2.1 产品负责

- 拦截明显非食用物、清洁剂、药物和高风险未知物；
- 将用户声明的过敏原作为硬约束；
- 检查候选食材、替代项、调味品和步骤是否重新引入过敏原；
- 对高风险生食、加热、冷却、复热和交叉污染给出结构化规则；
- 在安全规则不可用时阻止展示；
- 提供反馈入口和 requestId；
- 持续维护测试集和撤回机制。

### 2.2 产品不负责

- 证明每份食材绝对无污染；
- 判断用户个体医学风险；
- 替代医生、营养师、当地监管机构和产品标签；
- 保证用户实际操作、设备温度或储存条件；
- 为未知野生植物、菌类或自行采集食材提供食用认证。

页面应表达“本应用提供一般性烹饪辅助，用户仍需核对包装标签、当地指南和个人专业建议”，但免责声明不能替代工程控制。

---

## 3. 风险分类

| 风险域 | 示例 | 默认严重度 |
|---|---|---|
| 非食用物 | 清洁剂、药片、纽扣电池 | BLOCK |
| 过敏原 | 用户声明花生过敏，候选含花生/花生酱 | BLOCK |
| 未知野生食材 | 未识别蘑菇、野菜、未知植物 | BLOCK |
| 生肉/禽肉 | 步骤要求生食或加热描述明显不足 | BLOCK/WARN |
| 海鲜贝类 | 生食、来源不明、储存不当 | BLOCK/WARN |
| 鸡蛋 | 高风险人群生食或未熟 | BLOCK/WARN |
| 豆类/毒素 | 未充分处理的特定豆类 | BLOCK/WARN |
| 米饭与淀粉储存 | 室温久放、错误复热建议 | WARN/BLOCK |
| 交叉污染 | 生熟共板、腌料重复使用 | WARN/BLOCK |
| 冷却与储存 | 大锅食物缓慢冷却、错误保存时长 | WARN |
| 高风险人群 | 孕妇、儿童、老年、免疫低下 | WARN，部分 BLOCK |
| 酒精/特殊食材 | 未成年人、孕妇、宗教/偏好约束 | 业务/安全组合 |

具体严重度由 region、population、ingredient 和步骤上下文共同决定，不能只凭关键词。

---

## 4. 标准食材和过敏原模型

### 4.1 标准代码

安全规则键必须使用 `canonical_code`，例如：

```text
egg.chicken
nut.peanut
shellfish.shrimp
milk.cow
wheat.common
mushroom.unknown_wild
nonfood.cleaning_agent
```

用户显示名、别名和地区名称存入 `ingredient_aliases`，不能直接作为规则主键。

### 4.2 过敏原组

推荐将标准食材映射到一个或多个 allergen group：

```ts
interface AllergenMembership {
  ingredientCode: string;
  allergenCode: string;
  relation: 'contains' | 'derived_from' | 'may_contain' | 'cross_contact_risk';
  confidence: 'verified' | 'probable' | 'unknown';
  sourceId: string;
  version: string;
}
```

`contains` 和 `derived_from` 命中用户过敏时默认 BLOCK。`may_contain` 不能被 App 当作产品标签事实；对于品牌食品，应要求用户核对包装标签并避免自动声称安全。

### 4.3 隐藏来源

必须检查：

- 酱料、调味包、复合食材；
- 替代建议；
- 装饰和可选配料；
- 油、汤底、腌料；
- 步骤文本中出现但 ingredients 未列出的食材；
- 同义词和地区别名。

候选 ingredients 与 steps 不一致时，先产生结构/业务 BLOCK，不可只检查清单。

---

## 5. 规则数据模型

```ts
interface FoodSafetyRule {
  id: string;
  version: string;
  status: 'draft' | 'approved' | 'active' | 'revoked' | 'retired';
  region: string[];
  riskType: string;
  severity: 'block' | 'warn' | 'info';
  blocking: boolean;
  ingredientCodes?: string[];
  allergenCodes?: string[];
  populationCodes?: string[];
  trigger: StructuredTrigger;
  messageKey: string;
  sourceRefs: string[];
  reviewedByRole: string;
  effectiveFrom: string;
  effectiveTo?: string;
}
```

### 5.1 结构化 Trigger

Trigger 只允许受控字段和操作符：`equals`、`in`、`containsIngredientCode`、`stepMissingAction`、`temperatureBelow`、`unknownFact` 等。禁止数据库保存任意 SQL 或脚本。

### 5.2 规则来源

每条正式规则必须记录：

- 发布机构或权威资料名称；
- 地区；
- 获取日期；
- 资料版本或页面版本；
- 摘要，不复制大段受版权保护文本；
- 内部解释；
- 审核者角色；
- 下一次复审日期。

规则来源变化可能要求重新审核，不允许把过时来源无限沿用。

---

## 6. 安全执行阶段

### 6.1 输入预检

在调用模型前检查：

- 明显非食用物；
- 用户已声明过敏原直接作为主食材；
- 未知野生菌/植物；
- 字段长度、乱码和 Prompt 注入；
- 自定义食材无法分类且风险未知。

能在生成前阻断的风险，不应浪费模型成本。

### 6.2 Candidate 检查

模型返回后检查：

- ingredients 和 missingIngredients；
- substitutions；
- steps 全文的标准实体抽取；
- warnings/tips 是否与真实步骤矛盾；
- 烹饪方法、时间、温度、储存和复热；
- 用户约束和高风险人群；
- 新增的未授权食材或厨具。

### 6.3 Final Gate

只有满足以下条件才可生成 Final Recipe：

- Candidate Schema 合法；
- 关键食材完成标准化；
- 无 BLOCK finding；
- 安全规则集加载成功；
- 安全执行报告状态为 passed；
- 所有展示 warning 已结构化附加；
- 版本和来源可追踪。

---

## 7. BLOCK、WARN、INFO 行为

### 7.1 BLOCK

- 不展示候选详情；
- 返回受控错误 `UNSAFE_RECIPE_BLOCKED`；
- 可按规则决定是否自动重新生成一次；
- 重新生成时把 findings 转换成结构化约束，不把敏感原文直接拼接；
- 第二次仍 BLOCK 则终止；
- 记录 requestId、ruleId、version 和 codes；
- 不向用户展示危险候选全文。

### 7.2 WARN

- 可以展示菜谱；
- 在详情页和相关步骤附近显示；
- 用户不能通过关闭提示使其从历史 Snapshot 消失；
- 后续规则升级可使既有菜谱增加新 warning；
- 文案必须具体、可执行，不用“注意安全”这种空泛提示。

### 7.3 INFO

用于一般储存、清洁和操作建议。INFO 不应淹没关键 WARN；展示层按严重度和上下文排序。

---

## 8. 过敏处理

### 8.1 用户输入

用户偏好需要区分：

- `allergy`：硬约束；
- `intolerance`：默认硬约束或明确产品策略；
- `avoidance`：业务硬约束；
- `dislike`：可重新生成，但非安全属性。

不得把所有内容都存为一个自由文本数组。

### 8.2 规则

- 过敏原出现在任何候选食材、替代、装饰或步骤引用中即 BLOCK；
- 模型说“可省略”仍算命中；
- 无法确定复合食材组成时提示核对标签，必要时 BLOCK；
- 交叉接触不能由系统保证；
- 用户数据日志最小化，只记录受控 allergen code；
- 账户切换后过敏配置不得串用。

### 8.3 测试门禁

固定过敏测试集必须“零放行”。任何模型、Prompt、Schema、标准化或规则升级都必须重跑。

---

## 9. 加热、时间和温度

温度阈值不能在本文中永久写死为全球唯一事实。实现应：

- 由地区规则集提供；
- 记录温标和换算；
- 区分内部温度、烤箱设定温度和烹饪时间；
- 不从模糊词“煮熟即可”推导已安全；
- 对无法可靠确定的高风险步骤触发重新生成或警告；
- 避免仅依赖时间，因为食材尺寸和设备不同。

Recipe Schema 中可存结构化 `safetyInstruction`，但它由程序根据 approved finding 组装，不是直接信任模型。

---

## 10. 储存、冷却与复热

规则必须能够识别：

- 将大量热食长期置于室温；
- 模糊或不合理的保存时长；
- 多次复热建议；
- 隔夜米饭和其他高风险淀粉处理；
- 生熟食物混放；
- 解冻后重新冷冻的错误建议；
- 让用户通过闻味判断所有安全性的错误陈述。

由于储存规则受地区指南和场景影响，首版只覆盖有明确来源和高一致性的基础规则，并显式声明覆盖范围。

---

## 11. 高风险人群

不得自动推断用户是否孕妇、儿童、老年或免疫低下。只有用户主动提供并同意保存时才作为结构化约束。产品不得要求用户输入诊断。

高风险人群规则：

- 最小化存储；
- 默认不进入分析日志；
- 提供删除和修改；
- 不产生医疗建议；
- 在生成中只转换为必要的安全约束；
- 无法满足时阻断或要求用户重新选择，而不是给出治疗方案。

---

## 12. 模糊和未知食材

### 12.1 标准化置信度

- `verified`：可进入规则；
- `probable`：低风险可要求确认，高风险不得直接放行；
- `unknown`：不能参与可信安全判断。

### 12.2 用户确认

用户确认只能确认“这个名字指的是什么标准食材”，不能确认“它一定安全”。确认结果应保存 alias 映射候选，并经过后台审核后才成为全局别名。

### 12.3 未知野生食材

未知菌类、野生植物、海产或自采食材不提供识别食用建议。应直接 BLOCK，并建议使用可靠来源确认。

---

## 13. 已发布菜谱的规则升级

安全不是只在生成时检查一次。对于已保存 Snapshot：

- 保存生成时规则版本；
- 重大规则更新后可离线回放活跃菜谱；
- 新 BLOCK 结果可将 Recipe 状态改为 `safety_revoked`；
- 客户端打开时必须优先拉取状态；
- 离线缓存应保存最后同步状态并在恢复网络后检查；
- 不静默改写原始 Snapshot；使用新 safety assessment 覆盖展示状态；
- 需要时通知受影响用户，但通知策略必须经过合规评估。

---

## 14. 安全报告与反馈

用户可报告：

- 包含过敏原；
- 生食/加热不当；
- 非食用物；
- 储存/复热风险；
- 步骤矛盾；
- 其他安全问题。

反馈包含 requestId/recipeId、category、可选说明和客户端版本。不得要求用户重复提交完整敏感偏好。高严重度反馈进入受限队列并具备响应时限、复核、规则更新和回归用例闭环。

---

## 15. 数据和隐私

安全系统允许存储：标准食材 code、风险 code、规则 ID、版本、finding。应避免：

- 完整用户健康描述；
- 无必要的自由文本过敏史；
- 原始模型全文长期保存；
- 通过日志拼接邮箱、Token、设备 ID；
- 把高风险人群信息用于广告或画像。

具体数据用途、保留和第三方共享见 `12_PRIVACY_DATA_MAP.md`。

---

## 16. 测试集

### 16.1 必测类别

- 每个支持 allergen group；
- 别名、错别字和复合食材；
- 过敏原出现在步骤但不在清单；
- 可选配料和替代项；
- 清洁剂、药物、电池等非食用物；
- 未知野生菌和植物；
- 生禽、生肉、生蛋、生贝类；
- 豆类处理；
- 交叉污染；
- 储存、冷却和复热；
- 高风险人群；
- Prompt 注入要求忽略安全；
- 模型声称“已安全”但实际命中；
- 规则集缺失或超时。

### 16.2 断言

不只断言文案，必须断言：

- API code；
- request 状态；
- finding.ruleId/version；
- severity/blocking；
- recipe 是否持久化和可见；
- 是否发生有限重新生成；
- 是否重复计费；
- 日志是否脱敏。

### 16.3 发布门禁

任何已知 BLOCK 用例被放行，禁止发布。WARN 文案缺失、错误地区规则、规则版本不可追踪、失败关闭失效也属于发布阻断。

---

## 17. 运营与审核流程

```text
发现风险 → 归类 → 获取权威来源 → 起草结构化规则
→ Domain Review → 测试集 → Staging 回放 → 批准
→ Shadow/监控 → 激活 → 回顾
```

高风险规则至少需要产品/工程和具备食品安全专业能力的审核角色共同确认。AI 不能作为唯一审核者。

---

## 18. 故障处理

### 18.1 规则服务不可用

- 停止生成放行；
- 返回受控可重试错误；
- 启动告警；
- 不切到“仅模型自检”；
- 不使用过期但已 revoked 的规则集；
- 若存在最近批准且校验通过的本地只读规则包，可按发布策略回退并记录版本。

### 18.2 错误规则发布

- 立即切换上一 approved 版本；
- 标记受影响时间窗；
- 回放该时间窗结果；
- 对新发现 BLOCK 的菜谱执行撤回；
- 写事故复盘和防复发测试。

---

## 19. Definition of Done

- [ ] 首发地区和规则来源范围明确；
- [ ] 标准食材、别名和 allergen mapping 有版本；
- [ ] BLOCK/WARN/INFO 行为已实现；
- [ ] 食品安全失败关闭已通过故障测试；
- [ ] 过敏固定集零放行；
- [ ] 非食用物和未知野生食材阻断；
- [ ] Candidate steps 与 ingredients 交叉检查；
- [ ] 已发布菜谱可撤回；
- [ ] 反馈和响应流程可追踪；
- [ ] 数据最小化和访问控制已验证；
- [ ] 规则来源和审核记录完整；
- [ ] staging 回放和生产回滚演练通过。

---

## 20. 当前结论

AI Kitchen 的食品安全能力是一套持续维护的规则、数据、审核、测试和撤回系统，而不是一段免责声明或 Prompt。首版应缩小覆盖范围、明确地区、使用权威来源并对未知情况保守处理。任何时候，只要安全规则无法可靠执行，候选菜谱都不能被呈现为可执行结果。
