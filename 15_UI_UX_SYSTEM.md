# 15 — UI/UX System

> 本文定义 AI Kitchen 的信息架构、设计 Token、组件状态、可访问性、错误体验和烹饪场景规则。视觉风格可以后续调整，但语义、状态和安全提示层级必须稳定。

| 属性 | 内容 |
|---|---|
| 文档状态 | Draft / Ready for Product Design |
| 实施状态 | 尚未建立 Figma、Token 包或组件库 |
| 依赖 | `01_PRODUCT_PRD.md`、`13_MOBILE_APP_ARCHITECTURE.md` |
| 最后更新 | 2026-07-27 |

---

## 1. 体验原则

1. 用户在一分钟内理解如何从已有食材生成菜谱；
2. 厨房新手不需要理解 AI、Schema 或规则引擎；
3. 约束在生成前尽可能明确，而不是生成后才报错；
4. 安全信息高于促销、图片和装饰；
5. 所有异步流程有 loading/success/empty/error/retry；
6. 不用颜色作为唯一信息；
7. 烹饪模式支持湿手、单手、大字体和低注意力；
8. 用户知道系统做了什么、为什么失败、如何恢复；
9. 营养和 AI 能力不过度承诺；
10. guest 可以体验核心价值，登录是升级而不是入口门槛。

---

## 2. 信息架构

### Bottom Tabs

- 首页：食材和生成入口；
- 历史：最近生成；
- 收藏：用户主动保存；
- 我的：偏好、账户、隐私、反馈。

P0 可简化收藏/我的，但路由结构预留，不用将所有能力塞首页。

### 核心页面

1. 首页/食材选择；
2. 搜索与手动添加；
3. 生成条件；
4. 生成中；
5. 菜谱详情；
6. 烹饪模式；
7. 历史/收藏；
8. 偏好；
9. 反馈；
10. 隐私/账户删除。

---

## 3. 核心流程

```text
首页选择食材
→ 打开生成条件
→ 检查人数/时间/厨具/过敏
→ 生成中（可取消）
→ 详情（安全提示优先）
→ 开始烹饪
→ 完成/收藏/反馈
```

返回行为：

- 详情返回保留生成条件；
- 生成中返回需要确认取消，不自动产生第二请求；
- 烹饪中返回提示保存进度；
- 表单关闭保留草稿；
- 登录升级后返回原流程。

---

## 4. Design Tokens

```text
color.semantic.*
typography.*
space.*
radius.*
size.touchTarget
shadow.*
motion.duration.*
motion.easing.*
zIndex.*
```

禁止组件直接散落 hex、任意字号和间距。Token 支持 light/dark/high-contrast 语义，但首版不必同时设计所有主题。

### 4.1 语义颜色

- surface/background；
- text primary/secondary/disabled；
- action primary/secondary/destructive；
- status success/info/warning/danger；
- safety block/warn/info；
- border/focus/overlay。

`safety.block` 不等于普通 `destructive`，以便将食品安全与删除操作区分。

---

## 5. 排版

- 正文默认易读，不用过细字体；
- 步骤正文优先大字号和高行距；
- 数字、单位、计时器使用 tabular numerals（可用时）；
- 支持系统字体缩放；
- 不在固定高度容器中截断安全提示；
- 标题层级不依赖视觉大小，保持可访问语义；
- 中英文长度变化不破坏布局。

---

## 6. 组件分层

### Primitives

Text、Icon、Pressable、Stack、Divider、Surface。

### Controls

Button、TextField、SearchField、Chip、Checkbox、SegmentedControl、Slider、Stepper。

### Feedback

InlineError、Banner、EmptyState、Skeleton、Progress、Dialog、Toast。

### Domain Components

IngredientChip、RecipeCard、SafetyFindingCard、NutritionSummary、CookingStepCard、TimerPanel、MissingIngredientList。

领域组件接收已经校验的 view model，不自行解析 API DTO。

---

## 7. 状态矩阵

每个交互组件至少考虑：default、pressed、focused、selected、disabled、loading、error。每个数据页面至少考虑：initial、loading、refreshing、success、empty、offline-cache、error、permission-denied、stale/revoked。

设计评审必须检查状态矩阵，不只审成功截图。

---

## 8. 首页与食材选择

- 分类用于发现，不限制搜索；
- 选中状态同时有图标/文本变化；
- 显示已选数量；
- 支持移除和清空确认；
- 搜索无结果时允许手动添加；
- 自定义食材标记“待识别”；
- 高风险未知食材在生成前提示确认或阻断；
- 未选择食材时生成按钮禁用并解释原因；
- 不默认把调味品视为用户已有。

---

## 9. 生成条件

字段分组：

- 必填：人数、最大时间；
- 厨具；
- 偏好：辣度、难度、饮食目标；
- 安全：过敏、忌口；
- 高级设置折叠。

过敏信息不能藏在深层设置且只用小字。保存偏好与仅本次使用应明确区分。

---

## 10. 生成中

- 明确“正在生成并校验”；
- 避免虚假精确进度百分比；
- 展示可理解阶段，但不暴露内部安全实现细节；
- 取消按钮可用；
- 超时后提示正在恢复状态；
- 不鼓励连续点击；
- App 返回前台后恢复同一 requestId；
- 错误提供具体动作和反馈 ID。

可使用轻量内容提示，但不能用滚动“AI 思考过程”伪装实际进度。

---

## 11. 菜谱详情信息顺序

1. 安全 BLOCK/撤回状态（若存在，停止展示执行内容）；
2. 安全 WARN；
3. 标题、简介和关键元数据；
4. 缺少食材；
5. 食材清单；
6. 步骤；
7. 替代和提示；
8. 营养估算与来源；
9. 收藏、烹饪、反馈。

危险提示不能被大图、广告或折叠层遮挡。

---

## 12. 食品安全 UI

### BLOCK

- 不展示危险步骤；
- 明确说明无法提供该结果；
- 提供修改条件/重新生成；
- 提供报告入口；
- 不给“仍然查看”按钮。

### WARN

- 顶部汇总；
- 相关步骤内联；
- 图标 + 标题 + 具体动作；
- 屏幕阅读器可读；
- 不可仅靠红色；
- 历史打开仍显示。

### INFO

避免与 WARN 混在一起造成警告疲劳。一般提示可在步骤或“保存与处理”区域。

---

## 13. 营养 UI

- 明确“估算”；
- 默认展示每份，可切整道菜；
- 显示 servings；
- 展示来源、置信度或覆盖说明；
- unavailable 时不显示 0；
- 低置信度减少小数；
- 不用健康评分替代具体信息；
- 不用红绿评价用户食物“好/坏”；
- 免责声明可访问但不遮挡主流程。

---

## 14. 烹饪模式

- 一屏聚焦当前步骤；
- 大触控区域；
- 上一步/下一步明显；
- 食材量可快速查看；
- 安全 WARN 固定在相关步骤；
- 计时器开始/暂停/重置有确认边界；
- 多计时器复杂度放入后续；
- 防止误退出；
- 支持屏幕常亮；
- 完成后记录，不强迫分享或评分。

---

## 15. 错误体验

错误由“发生了什么 + 用户可以做什么 + requestId（必要时）”组成。

示例类别：

- 输入错误：定位字段；
- 离线：查看缓存，联网后重试；
- AI 超时：恢复同一请求；
- 限流：显示可重试时间；
- 安全阻断：修改条件，不展示候选；
- Auth 过期：重新登录但保留草稿；
- 本地数据库：安全恢复/导出诊断，不静默清空；
- 系统错误：反馈 requestId。

Toast 只用于低风险、瞬时反馈，不承载关键错误和安全信息。

---

## 16. 空状态

空状态应解释“为什么空”和“下一步”：

- 历史为空：开始第一次生成；
- 收藏为空：从 Recipe 收藏；
- 搜索无结果：手动添加；
- 离线无缓存：连接网络；
- 账户升级后同步中：等待/查看本地；
- 被撤回 Recipe：说明无法继续使用。

---

## 17. 可访问性

- 最小触控目标；
- 屏幕阅读器 label/hint/state；
- 动态字体；
- 颜色对比；
- 键盘和焦点顺序（平板/辅助设备）；
- 减少动画设置；
- 安全提示 live region 或等效策略；
- 图标有文本替代；
- 倒计时不每秒打断屏幕阅读器；
- 横屏/大屏可用但不作为 P0 主要布局。

可访问性是 DoD，不是发布前补丁。

---

## 18. 国际化

首版可只发布一种语言，但代码从第一天使用 message keys：

- 不在组件拼接句子；
- 单位、数字、日期按 locale；
- 中文复数简单但英文需处理；
- 规则 messageKey 与参数分离；
- 长文本和 RTL 未来兼容；
- 用户输入原文与 canonical name 分离。

---

## 19. 动效

动效用于解释层级和状态：选择、页面过渡、计时器、Skeleton。禁止：

- 让生成看起来比实际更快的虚假完成；
- BLOCK 提示使用庆祝动效；
- 过多循环动画影响厨房场景；
- 忽略 reduce motion；
- 用动画隐藏布局跳动。

---

## 20. 设计交付

每个页面设计必须包含：

- 主状态和异常状态；
- 空/离线/错误；
- 字体放大；
- 安全提示；
- 深色模式影响（若支持）；
- Android/iOS 差异；
- 组件与 token 引用；
- 交互说明；
- 埋点（仅受控事件）；
- 验收标准。

---

## 21. UI 测试

- Screenshot/visual regression 用于稳定组件；
- 不依赖像素测试验证业务正确性；
- 组件交互测试状态；
- E2E 覆盖核心流程；
- 真机验证字体、键盘、后台、弱网；
- 安全提示和可访问性自动检查；
- 重要页面做小屏和大字体组合。

---

## 22. Definition of Done

- [ ] Token 和语义组件建立；
- [ ] 核心页面状态矩阵完整；
- [ ] 安全提示不依赖颜色且层级正确；
- [ ] 生成、详情、烹饪错误恢复明确；
- [ ] 动态字体和屏幕阅读器基础通过；
- [ ] Android 小屏和真机通过；
- [ ] 营养文案无精确/医疗承诺；
- [ ] guest 主流程不被登录阻断；
- [ ] 设计、实现和测试引用同一状态定义；
- [ ] UI 变更记录和回滚可执行。

---

## 23. 当前结论

AI Kitchen 的 UI 系统首先服务“可执行、可理解和安全”，其次才是视觉氛围。品牌、插画和动效可以迭代，但页面状态、安全层级、可访问性和恢复行为必须在编码前明确，并通过统一 token 和领域组件实现。
