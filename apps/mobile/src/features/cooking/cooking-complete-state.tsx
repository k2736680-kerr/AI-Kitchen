import { AppButton } from '@/components/app-button';
import { useTranslation } from 'react-i18next';
import { ThemedText } from '@/components/themed-text';
export function CookingCompleteState({ title, completed, total, onDetail, onRestart, onHome }: { readonly title: string; readonly completed: number; readonly total: number; readonly onDetail: () => void; readonly onRestart: () => void; readonly onHome: () => void }) { const { t } = useTranslation(); return <><ThemedText type="title">{t('cooking.completeTitle')}</ThemedText><ThemedText>{title}</ThemedText><ThemedText themeColor="textSecondary">{t('cooking.completed', { completed, total })}</ThemedText><ThemedText themeColor="textSecondary">{t('cooking.completeHint')}</ThemedText><AppButton label={t('cooking.recipeDetail')} onPress={onDetail} /><AppButton label={t('cooking.restart')} variant="secondary" onPress={onRestart} /><AppButton label={t('common.home')} variant="ghost" onPress={onHome} /></>; }
