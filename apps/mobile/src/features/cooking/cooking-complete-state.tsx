import { AppButton } from '@/components/app-button';
import { ThemedText } from '@/components/themed-text';
export function CookingCompleteState({ title, completed, total, onDetail, onRestart, onHome }: { readonly title: string; readonly completed: number; readonly total: number; readonly onDetail: () => void; readonly onRestart: () => void; readonly onHome: () => void }) { return <><ThemedText type="title">菜谱步骤已完成</ThemedText><ThemedText>{title}</ThemedText><ThemedText>已完成步骤：{completed} / {total}</ThemedText><ThemedText>烹饪进度仅保存在当前应用会话。</ThemedText><AppButton label="返回菜谱详情" onPress={onDetail} /><AppButton label="重新开始" variant="secondary" onPress={onRestart} /><AppButton label="返回首页" variant="ghost" onPress={onHome} /></>; }
