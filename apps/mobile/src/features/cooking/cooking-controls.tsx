import { Alert } from 'react-native';
import { AppButton } from '@/components/app-button';
export function CookingControls({ current, total, completeDisabled, onPrevious, onNext, onComplete, onExit }: { readonly current: number; readonly total: number; readonly completeDisabled: boolean; readonly onPrevious: () => void; readonly onNext: () => void; readonly onComplete: () => void; readonly onExit: () => void }) { return <><AppButton label="上一步" variant="secondary" disabled={current === 0} onPress={onPrevious} /><AppButton label={completeDisabled ? '本步骤已完成' : '完成本步骤'} onPress={onComplete} disabled={completeDisabled} /><AppButton label="下一步" variant="secondary" disabled={current >= total - 1} onPress={onNext} /><AppButton label="退出烹饪" variant="ghost" onPress={() => Alert.alert('确定退出当前烹饪吗？', '当前进度会保留在本次应用会话中。', [{ text: '继续烹饪', style: 'cancel' }, { text: '退出并返回详情', onPress: onExit }])} /></>; }
