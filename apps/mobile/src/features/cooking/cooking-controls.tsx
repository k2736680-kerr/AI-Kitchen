import { Alert } from 'react-native';
import { useTranslation } from 'react-i18next';
import { AppButton } from '@/components/app-button';
export function CookingControls({ current, total, completeDisabled, onPrevious, onNext, onComplete, onExit }: { readonly current: number; readonly total: number; readonly completeDisabled: boolean; readonly onPrevious: () => void; readonly onNext: () => void; readonly onComplete: () => void; readonly onExit: () => void }) { const { t } = useTranslation(); return <><AppButton label={t('cooking.previous')} variant="secondary" disabled={current === 0} onPress={onPrevious} /><AppButton label={completeDisabled ? t('cooking.completedStep') : t('cooking.complete')} onPress={onComplete} disabled={completeDisabled} /><AppButton label={t('cooking.next')} variant="secondary" disabled={current >= total - 1} onPress={onNext} /><AppButton label={t('cooking.exit')} variant="ghost" onPress={() => Alert.alert(t('cooking.exitTitle'), t('cooking.exitHint'), [{ text: t('cooking.continue'), style: 'cancel' }, { text: t('cooking.exitToDetail'), onPress: onExit }])} /></>; }
