import { ThemedText } from '@/components/themed-text';
export function CookingHeader({ title, current, total, completed, status }: { readonly title: string; readonly current: number; readonly total: number; readonly completed: number; readonly status: 'in-progress' | 'completed' }) { return <><ThemedText type="title">{title}</ThemedText><ThemedText>分步烹饪</ThemedText><ThemedText>第 {current + 1} 步，共 {total} 步</ThemedText><ThemedText>已完成 {completed} / {total}</ThemedText><ThemedText>状态：{status === 'completed' ? '已完成' : '进行中'}</ThemedText></>; }
