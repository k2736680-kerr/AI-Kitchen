import { ThemedText } from '@/components/themed-text';
import { useTranslation } from 'react-i18next';
export function CookingHeader({ title, current, total, completed }: { readonly title: string; readonly current: number; readonly total: number; readonly completed: number; readonly status: 'in-progress' | 'completed' }) { const { t } = useTranslation(); return <><ThemedText type="title">{title}</ThemedText><ThemedText type="small" themeColor="textSecondary">{t('cooking.eyebrow')}</ThemedText><ThemedText>{t('cooking.stepOf', { current: current + 1, total })}</ThemedText><ThemedText type="small" themeColor="textSecondary">{t('cooking.completed', { completed, total })}</ThemedText></>; }
